{
  "name": "ais-drone-server",
  "version": "2.0.0",
  "description": "AIS backend: dAISy serial + TCP + UDP → WebSocket",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "serialport": "^12.0.0"
  }
}
